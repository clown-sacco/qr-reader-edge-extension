# 更新日志 / Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。日期为提交日期。

## [1.1.0] - 2026-09-20

### 变更

- **去掉自动复制**：识别结果改为弹窗展示，不再自动写入剪贴板
- 结果气泡提供手动「复制」「打开链接」按钮，操作后气泡自动关闭
- 气泡悬浮时间统一为 5 秒（原为成功 8 秒 / 出错 6 秒）
- 气泡动画确定为「弹跳下落」（drop），退出时反向播放；备选动画可在 `test/toast-preview.html` 预览，通过 `content.js` 的 `ANIM` 常量切换

### 移除

- 删除 offscreen 离屏文档及 `offscreen`、`clipboardWrite` 权限（此前仅为后台自动复制而存在）

## [1.0.0] - 2026-09-20

### 首个版本

- 右键图片识别二维码，jsQR 本地解码
- 支持跨域图片（后台抓取 + 元素绘制 + CORS 重载三层取图策略）
- 超大图等比缩放（最长边 2400px）后解码，反色二维码可识别
- 结果气泡展示 + 自动复制（后被 1.1.0 移除）
- 解码验证测试（URL / 中文文本 / 反色 / 缩放路径）
